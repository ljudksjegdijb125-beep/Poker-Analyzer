/* =========================================================
   POKEJI V45.4.1 · real phone application interiors
   Existing desktop, wallpaper, icon layout and colors are not rebuilt.
   ========================================================= */
(function(){
  'use strict';
  if(window.__pokejiV455PhoneLoaded)return;
  window.__pokejiV455PhoneLoaded=true;
  const V=window.V455;if(!V)return;
  const {S,O,E,AT,A,NOW,uid,clone,save,personaFor,directCharacter,phoneSession,ownerEntity,ownerName,actualActor,permissionAtLeast,canCreate,canSend,ownerStore,entityFromToken,avatarMarkup,appName,setPhoneContent,phoneStatus,worldTime,array}=V;
  const basePhoneApp=typeof v43OpenPhoneApp==='function'?v43OpenPhoneApp:null;
  let owner='user',app='messages',threadId='',postId='',photoId='',walletFilter='all',galleryTab='photos',selectedMessages=new Set(),deleteMode='silent',phoneCompletionDraft=null,phoneCompletionBusy=false;

  function store(){return ownerStore(owner)}
  function real(){return store().realAppsV455}
  function session(){return phoneSession()}
  function phoneClass(){return owner==='user'?'is-user':'is-character'}
  function head(title,backAction=`v43PhoneDesktop(${A(owner)})`,right='',rightAction=''){
    return`${phoneStatus()}<header class="vphone-app-head v455-phone-head"><button onclick="${backAction}" aria-label="返回">‹</button><h2>${E(title)}</h2><button onclick="${rightAction||'closePhone()'}">${right?E(right):'×'}</button></header>`
  }
  function shell(title,body,{backAction,right,rightAction,bodyClass=''}={}){setPhoneContent(`<div class="vphone vphone-app ${phoneClass()} v455-phone-shell">${head(title,backAction,right,rightAction)}<main class="vphone-app-body v452-app-body v455-phone-body ${bodyClass}">${body}</main></div>`);setTimeout(()=>{try{window.v453ShieldTextFields?.(document.querySelector('.v455-phone-shell'))}catch{}},0)}
  function empty(icon,title,copy,action='',actionLabel=''){return`<div class="v455-phone-empty"><span>${icon}</span><b>${E(title)}</b><p>${E(copy)}</p>${action?`<button onclick="${action}">${E(actionLabel)}</button>`:''}</div>`}
  function ownerAvatar(className='v455-phone-avatar'){return avatarMarkup(ownerEntity(owner),ownerName(owner),className)}
  function imageSource(value){try{return typeof safeImageSrc==='function'?safeImageSrc(value):S(value)}catch{return''}}
  function displayTime(value){if(!value)return'';const date=new Date(value);if(Number.isNaN(date.getTime()))return S(value);return date.toLocaleString('zh-CN',{month:'numeric',day:'numeric',hour:'2-digit',minute:'2-digit'})}
  function allowEdit(){return canCreate(owner,session())}
  function requireEdit(){if(allowEdit())return true;toast(session().mode==='reverse'?'本次反查连接没有修改权限':'当前手机不允许手动修改');return false}
  function requireSend(){if(canSend(owner,session()))return true;toast('本次连接为只读，不能发送消息');return false}
  function recordOperation(operation,details={},remember=false){if(typeof v454RecordPhoneEvent!=='function')return;const s=session();v454RecordPhoneEvent({phoneOwner:owner,actualActor:actualActor(s),operation,permission:s.permission||'none',localChanged:details.localChanged===true,remoteSynced:details.remoteSynced===true,remember,details:{summary:S(details.summary)}})}

  /* ---------- page-local role reaction, never a contact bubble ---------- */
  function reactionKey(scope){const s=session();return`${s.mode}:${owner}:${scope}`}
  function reactionMarkup(scope,visible){const s=session();if(!['check','reverse'].includes(s.mode))return'';s.replies=O(s.replies);const character=V.currentCharacterForSession(s),key=reactionKey(scope),cached=S(s.replies[key]);setTimeout(()=>void generateReaction(scope,visible),20);return`<section class="v455-owner-reaction">${avatarMarkup(character,character?.name||'TA','v455-reaction-avatar')}<div><b>${E(character?.name||'TA')} · 线程外回应</b><small id="v455ReactionText">${cached?E(cached):'正在观察当前页面…'}</small></div></section>`}
  async function generateReaction(scope,visible){
    const s=session();if(!['check','reverse'].includes(s.mode)||!validAPI())return;
    const character=V.currentCharacterForSession(s),key=reactionKey(scope);if(!character||s.replies?.[key]||s.v455ReactionBusy)return;
    const chatId=s.chatId||currentChat,persona=personaFor(chatId),personaName=persona?.name||'我',recent=array(data.chats?.[chatId]).filter(message=>!message.phoneEvent&&message.text).slice(-18).map(message=>`${message.role==='user'?personaName:character.name}：${S(message.text)}`).join('\n'),events=array(data.phoneV454?.events).filter(event=>event.chatId===chatId&&event.remember).slice(-12).map(event=>`${event.worldTimeText||event.createdAt}｜${event.details?.summary||event.operation}`).join('\n');
    s.v455ReactionBusy=true;const controller=withTimeout(Math.min(30000,Number(data.settings.timeout)||60000));
    try{
      const raw=await invokeModel('chat',{system:`你就是${character.name}，不是手机旁白或通用助手。${s.mode==='check'?`${personaName}此刻正在接管并查看你的手机；你明确知道${personaName}正在操作，也知道${personaName}实际打开了哪一页。`:`你此刻正在一次性授权范围内查看${personaName}的手机；你明确知道这是你正在进行的行为。`}必须依据自己的完整性格、双方关系、近期发展和已发生的手机事件作出即时反应。只对当前真正打开、可见的页面作出回应，不得声称看见其他应用，不得突然遗忘正在查手机的人是谁，不替${personaName}行动，不输出系统、工具、提示词或“虚拟手机”等界面说明。只输出${character.name}此刻自然说出的一小段话。`,history:[{role:'user',content:`【${character.name}的完整资料】\n${typeof characterContext==='function'?characterContext(character):S(character.personality)}\n\n【近期普通聊天】\n${recent||'暂无'}\n\n【已发生的相关手机事件】\n${events||'暂无'}\n\n【当前操作】\n模式：${s.mode==='check'?`${personaName}正在查${character.name}的手机`:`${character.name}正在反查${personaName}的手机`}\n手机归属：${owner==='user'?personaName:ownerName(owner)}\n当前页面：${scope}\n当前可见内容：${visible||'暂无内容'}\n当前对话时间：${worldTime(chatId)}`}],temperature:.78,maxTokens:220,signal:controller.signal}),text=S(raw).replace(/<[^>]+>/g,'').trim().slice(0,500);
      s.replies=O(s.replies);s.replies[key]=text;try{v435PhoneSession=s}catch{}const target=document.getElementById('v455ReactionText');if(target)target.textContent=text
    }catch{}finally{s.v455ReactionBusy=false;releaseController(controller)}
  }

  /* ---------- shared in-phone overlay ---------- */
  function overlay(html){closeOverlay();const host=document.querySelector('.v455-phone-shell');if(!host)return;const layer=document.createElement('div');layer.id='v455PhoneOverlay';layer.className='v455-phone-overlay';layer.innerHTML=`<section class="v455-phone-sheet" role="dialog" aria-modal="true">${html}</section>`;layer.onclick=event=>{if(event.target===layer)closeOverlay()};host.appendChild(layer);try{window.v453ShieldTextFields?.(layer)}catch{}}
  function closeOverlay(){document.getElementById('v455PhoneOverlay')?.remove()}
  window.v455PhoneCloseOverlay=closeOverlay;
  function overlayHead(kicker,title){return`<header><div><small>${E(kicker)}</small><h2>${E(title)}</h2></div><button onclick="v455PhoneCloseOverlay()">×</button></header>`}
  function actions(saveAction,saveLabel='保存',danger=''){return`<footer>${danger}<button onclick="v455PhoneCloseOverlay()">取消</button><button class="primary" onclick="${saveAction}">${E(saveLabel)}</button></footer>`}

  /* ---------- real messages: list -> detail ---------- */
  function threadContact(thread){
    if(thread.contactToken){const entity=entityFromToken(thread.contactToken);if(entity)return entity}
    if(thread.contactId)return data.characters?.find(x=>x.id===thread.contactId)||data.mpcs?.find(x=>x.id===thread.contactId)||data.personas?.find(x=>x.id===thread.contactId)||null;
    return data.characters?.find(x=>x.name===thread.title)||data.mpcs?.find(x=>x.name===thread.title)||null
  }
  function threadAvatar(thread,className='v455-phone-avatar'){if(thread.kind==='group'){const group=threadContact(thread)||data.groups?.find(x=>x.id===thread.contactId);return avatarMarkup(group,thread.title||'群',className)}return avatarMarkup(threadContact(thread),thread.title||'联',className)}
  function lastMessage(thread){return thread.messages?.at(-1)||null}
  function openMessages(){app='messages';threadId='';const s=store(),rows=s.conversations.slice().sort((a,b)=>(b.pinned===true)-(a.pinned===true)||S(b.updatedAt).localeCompare(S(a.updatedAt))).map(t=>{const last=lastMessage(t);return`<button class="v455-conversation-row" onclick="v455PhoneOpenThread(${A(t.id)})">${threadAvatar(t)}<span><span><b>${E(t.title)}</b>${t.kind==='group'?'<i>群聊</i>':''}${t.pinned?'<i>置顶</i>':''}${t.unread?`<em>${Math.min(99,t.unread)}</em>`:''}</span><small>${E(t.typing?'正在输入…':S(last?.localText||last?.text||'暂无消息'))}</small></span><time>${E(last?.worldTimeText||displayTime(t.updatedAt))}</time></button>`}).join('');shell('聊天',`${reactionMarkup('messages-list',`${s.conversations.length} 条手机会话`)}<div class="v455-phone-search">⌕　搜索联系人、群聊或消息</div>${rows?`<div class="v455-conversation-list">${rows}</div>`:empty('◇','还没有手机会话','可从联系人白名单建立第一条独立手机会话。',`v455PhoneOpenContacts()`,'管理联系人')}<div class="v455-app-actions"><button onclick="v455PhoneOpenContacts()">联系人</button><button onclick="v455PhoneOpenMessageManagement()">聊天管理</button></div>`,{right:'管理',rightAction:'v455PhoneOpenMessageManagement()',bodyClass:'v455-messages-body'})}
  window.v455PhoneOpenMessages=openMessages;
  function senderEntity(thread,message){if(message.senderId)return data.characters?.find(x=>x.id===message.senderId)||data.mpcs?.find(x=>x.id===message.senderId)||null;if(message.senderToken)return entityFromToken(message.senderToken);if(thread.kind==='group'&&message.actualActor)return data.characters?.find(x=>x.id===message.actualActor)||data.mpcs?.find(x=>x.id===message.actualActor)||null;return threadContact(thread)}
  function messageText(message){return S(message.localText??message.text)}
  function blockCharacterForThread(thread){const token=S(thread.contactToken),[kind,id]=token.split(':');if(owner==='user'&&kind==='character')return id;if(owner!=='user'&&kind==='persona')return owner;return''}
  function blockedForThread(thread){const characterId=blockCharacterForThread(thread);return characterId?window.v455BlockStatusForCharacter?.(characterId,V.personaFor()?.id):null}
  function outgoingRelationshipMark(relation){if(!relation)return'';return owner==='user'?(relation.characterBlocksUser?'blocked-by-character':''):(relation.userBlocksCharacter?'blocked-by-user':'')}
  function incomingRelationshipMark(relation){if(!relation)return'';return owner==='user'?(relation.userBlocksCharacter?'blocked-by-user':''):(relation.characterBlocksUser?'blocked-by-character':'')}
  function messageMarkup(thread,message){const outgoing=message.direction!=='incoming',entity=outgoing?ownerEntity(owner):senderEntity(thread,message),name=outgoing?ownerName(owner):(message.senderName||entity?.name||thread.title),warning=message.relationshipMark?`<button class="v455-phone-failed" onclick="event.stopPropagation();v456PhoneShowRelationshipMark(${A(thread.id)},${A(message.id)})">!</button>`:'';return`<div class="v455-thread-msg ${outgoing?'out':''}">${outgoing?'':avatarMarkup(entity,name,'v455-thread-avatar')}<div class="v455-thread-column"><small>${E(name)}</small><div class="v455-thread-bubble" onclick="v455PhoneOpenMessageMenu(${A(message.id)})">${E(messageText(message))}${warning}</div><time>${E(message.worldTimeText||displayTime(message.createdAt))}</time></div>${outgoing?avatarMarkup(entity,name,'v455-thread-avatar'):''}</div>`}
  function openThread(id){const thread=store().conversations.find(x=>x.id===id);if(!thread)return toast('会话不存在');threadId=id;thread.unread=0;save();const s=session(),canWrite=canSend(owner,s),content=thread.messages.length?thread.messages.map(m=>messageMarkup(thread,m)).join(''):'<div class="v455-thread-empty">这条会话还没有消息。</div>';shell(thread.title,`<section class="v455-thread-page">${reactionMarkup(`thread:${id}`,`正在查看与${thread.title}的聊天`)}<div class="v455-thread-scroll ${['check','reverse'].includes(s.mode)?'has-reaction':''}"><div class="v455-thread-time"><span>${E(worldTime(s.chatId||currentChat))}</span></div>${content}<div id="v455PhoneTyping"></div></div><footer class="v455-thread-composer"><button onclick="v455PhoneMessageTools()">＋</button><div id="v455PhoneInput" class="v455-phone-safe-input" contenteditable="${canWrite?'plaintext-only':'false'}" role="textbox" data-placeholder="${canWrite?'输入消息…':'当前为只读权限'}" data-form-type="other" data-lpignore="true"></div><button onclick="v455PhoneSendMessage()" ${canWrite?'':'disabled'}>发送</button></footer></section>`,{backAction:'v455PhoneOpenMessages()',right:'管理',rightAction:'v455PhoneMessageTools()',bodyClass:'v455-thread-host'})}
  window.v455PhoneOpenThread=openThread;
  function phoneTranscript(thread){return thread.messages.slice(-30).map(m=>`${m.direction==='incoming'?(m.senderName||thread.title):ownerName(owner)}：${messageText(m)}`).join('\n')}
  async function generateContactReply(thread){
    const contact=threadContact(thread);if(!contact||thread.contactToken?.startsWith('persona:')||!validAPI())return;const relation=blockedForThread(thread),ownerLabel=ownerName(owner),relationText=relation?.userBlocksCharacter?`${ownerLabel}已经把${contact.name||thread.title}加入黑名单；这是一段关系事实，但不妨碍本轮正常说话。`:relation?.characterBlocksUser?`${contact.name||thread.title}已经把${ownerLabel}加入黑名单；这是一段关系事实，但不妨碍本轮正常说话。`:'双方目前没有拉黑彼此。';thread.typing=true;save();const typing=document.getElementById('v455PhoneTyping');if(typing)typing.innerHTML='<div class="v455-phone-typing">对方正在输入</div>';const controller=withTimeout(Number(data.settings.timeout)||60000);
    try{let sender=contact,system='';if(thread.kind==='group'){const group=contact,memberId=(group.memberIds||[])[Number(group.turnIndex)||0]||(group.memberIds||[])[0];sender=data.characters?.find(x=>x.id===memberId)||contact;system=`你是群聊“${group.name}”中的${sender.name||'成员'}。只以该成员身份回复最新手机消息。`}else system=`你是${contact.name||thread.title}，正在手机聊天中回复。`;const raw=await invokeModel('chat',{system:`${system}${relationText} 回复必须自然，不能替${ownerLabel}、手机主人或其他成员行动。黑名单不是技术拦截，不得声称消息发送失败，也不能因此停止回应。只有存在可靠语气、关系或记录证据时才能怀疑实际操作者；禁止随机全知。只输出真正发出的消息文字。`,history:[{role:'user',content:`${sender.name||'联系人'}的资料：\n${typeof characterContext==='function'?characterContext(sender):JSON.stringify(sender)}\n手机归属者：${ownerLabel}\n当前时间：${worldTime()}\n消息记录：\n${phoneTranscript(thread)}`}],temperature:.8,maxTokens:500,signal:controller.signal}),reply=S(raw).replace(/<[^>]+>/g,'').trim();if(reply){thread.messages.push({id:uid('phone_message'),type:'text',direction:'incoming',text:reply,localText:reply,remoteText:reply,senderId:S(sender.id),senderName:S(sender.name||thread.title),actualActor:S(sender.id),displayActor:S(sender.id),worldTimeText:worldTime(),createdAt:NOW(),remoteSynced:true,relationshipMark:incomingRelationshipMark(relation),editHistory:[],modifyHistory:[]});thread.updatedAt=NOW();recordOperation('message-received',{localChanged:true,remoteSynced:true,summary:`${sender.name||thread.title}回复了手机消息`},true)}}catch(error){console.warn('V45.4.1 phone reply',S(error?.message||error))}finally{thread.typing=false;releaseController(controller);save();if(threadId===thread.id)openThread(thread.id)}}
  window.v455PhoneSendMessage=function(){if(!requireSend())return;const thread=store().conversations.find(x=>x.id===threadId),input=document.getElementById('v455PhoneInput'),text=S(input?.innerText??input?.textContent).trim();if(!thread||!text)return;const relation=blockedForThread(thread),s=session(),message={id:uid('phone_message'),type:'text',direction:'outgoing',text,localText:text,remoteText:text,worldTimeText:worldTime(),createdAt:NOW(),actualActor:actualActor(s),displayActor:owner,remoteSynced:true,relationshipMark:outgoingRelationshipMark(relation),editHistory:[],modifyHistory:[]};thread.messages.push(message);thread.updatedAt=NOW();save();openThread(thread.id);recordOperation('message-sent',{localChanged:true,remoteSynced:true,summary:`向${thread.title}发送手机消息`},true);void generateContactReply(thread)};
  window.v456PhoneShowRelationshipMark=function(tid,mid){const thread=store().conversations.find(x=>x.id===tid),message=thread?.messages.find(x=>x.id===mid);if(!message)return;const relation=blockedForThread(thread),personaName=personaFor()?.name||'我',characterName=data.characters?.find(item=>item.id===relation?.characterId)?.name||thread.title||ownerName(owner),copy=message.relationshipMark==='blocked-by-user'?`${personaName}当时已拉黑${characterName}，所以标记在${characterName}的气泡旁。`:`${characterName}当时已拉黑${personaName}，所以标记在${personaName}的气泡旁。`;overlay(`${overlayHead('RELATION MARK','关系标记')}<div class="v455-phone-warning">!</div><p class="v455-sheet-note">${E(copy)}这条消息仍然正常生成并保存在双方往来中，不代表发送失败。</p><p class="v455-sheet-note">当前：${E(relation?.userBlocksCharacter&&relation?.characterBlocksUser?'彼此互相拉黑':relation?.userBlocksCharacter?`${personaName}拉黑了${characterName}`:relation?.characterBlocksUser?`${characterName}拉黑了${personaName}`:'彼此没有拉黑')}。</p>${actions('v455PhoneCloseOverlay()','知道了')}`)};
  window.v455PhoneShowFailed=window.v456PhoneShowRelationshipMark;
  window.v455PhoneMessageTools=function(){const thread=store().conversations.find(x=>x.id===threadId);if(!thread)return v455PhoneOpenMessageManagement();overlay(`${overlayHead('CHAT TOOLS',thread.title)}<div class="v455-sheet-list"><button onclick="v455PhoneOpenContacts();v455PhoneCloseOverlay()"><b>联系人与白名单</b><small>管理可联系对象和主动说话</small></button><button onclick="v455PhoneOpenMessageManagement();v455PhoneCloseOverlay()"><b>聊天记录管理</b><small>编辑、删除、清空、回收站与重置</small></button><button onclick="v455PhoneCloseOverlay();v455PhoneOpenDiffList()"><b>本机 / 远端差异</b><small>旧记录本机改写不会自动同步远端</small></button><button onclick="v455PhoneToggleProactive(${A(thread.id)})"><b>主动说话：${thread.proactiveEnabled?'开启':'关闭'}</b><small>必须已有会话，并为当前联系人单独开启</small></button></div>${actions('v455PhoneCloseOverlay()','完成')}`)};
  window.v455PhoneToggleProactive=function(id){const thread=store().conversations.find(x=>x.id===id);if(!thread)return;thread.proactiveEnabled=!thread.proactiveEnabled;thread.updatedAt=NOW();save();closeOverlay();openThread(id);toast(thread.proactiveEnabled?'已允许该联系人主动说话':'已关闭该联系人主动说话')};

  /* Contacts and basic management retain advanced data boundaries. */
  function contactCandidates(){const list=[],seen=new Set(),world=V.currentWorld(),persona=personaFor(),worldChars=new Set(world?.characterIds||[]),worldMpcs=new Set(world?.mpcIds||[]),worldGroups=new Set(world?.groupIds||[]),add=(token,entity,kind)=>{if(!entity||seen.has(token))return;seen.add(token);list.push({token,entity,kind})};if(owner!=='user'&&persona)add(`persona:${persona.id}`,persona,'面具');
    for(const c of data.characters||[]){if(c.id===owner)continue;const related=c.boundPersonaId===persona?.id||(c.personaIds||[]).includes(persona?.id),inWorld=!world||!worldChars.size||worldChars.has(c.id);if(inWorld||related||c.id===directCharacter()?.id)add(`character:${c.id}`,c,'人物')}
    for(const m of data.mpcs||[]){const related=m.boundPersonaId===persona?.id||(m.personaIds||[]).includes(persona?.id),inWorld=!world||m.worldId===world.id||worldMpcs.has(m.id);if(inWorld||related)add(`mpc:${m.id}`,m,'世界联系人')}
    for(const g of data.groups||[]){const members=array(g.memberIds),inWorld=!world||worldGroups.has(g.id)||members.some(id=>worldChars.has(id));if(inWorld)add(`group:${g.id}`,g,'群聊')}
    for(const t of store().contacts||[])if(t.temporary)add(`temporary:${t.id}`,t,'临时联系人');return list
  }
  window.v455PhoneOpenContacts=function(){const s=store(),rows=contactCandidates().map(row=>{const on=s.whitelist.includes(row.token),thread=s.conversations.find(x=>x.contactToken===row.token);return`<article class="v455-contact-row">${avatarMarkup(row.entity,row.entity.name,'v455-phone-avatar')}<span><b>${E(row.entity.name||'未命名')}</b><small>${E(row.kind)}</small></span><div><button onclick="v455PhoneToggleWhitelist(${A(row.token)})">${on?'移出':'加入'}</button>${on?`<button class="primary" onclick="v455PhoneStartConversation(${A(row.token)})">${thread?'打开':'会话'}</button>`:''}${row.kind==='临时联系人'?`<button onclick="v455PhoneConvertTemporary(${A(row.entity.id)})">转为常驻联系人</button>`:''}</div></article>`}).join('');shell('联系人',`${owner==='user'&&session().mode==='browse'?`<section class="v456-phone-completion-entry"><span>✦</span><div><b>智能完善我的手机</b><small>可选一个、多个或全部白名单联系人；生成后先预览，只追加所选内容。</small></div><button onclick="v456PhoneOpenCompletion()">开始</button></section>`:''}${rows?`<div class="v455-contact-list">${rows}</div>`:empty('联','还没有联系人候选','请先建立人物或群聊。')}<div class="v455-app-actions"><button onclick="v455PhoneNewTemporaryContact()">＋ 临时联系人</button><button onclick="v455PhoneOpenMessages()">返回聊天</button></div>`,{backAction:'v455PhoneOpenMessages()',right:'完成',rightAction:'v455PhoneOpenMessages()'})};
  window.v455PhoneToggleWhitelist=function(token){if(!requireEdit())return;const s=store(),index=s.whitelist.indexOf(token);if(index>=0)s.whitelist.splice(index,1);else s.whitelist.push(token);save();v455PhoneOpenContacts()};
  window.v455PhoneStartConversation=function(token){const s=store();if(!s.whitelist.includes(token))return toast('请先加入白名单');let thread=s.conversations.find(x=>x.contactToken===token);if(!thread){const row=contactCandidates().find(x=>x.token===token);if(!row)return;thread={id:uid('phone_thread'),title:S(row.entity.name||'未命名会话'),kind:row.kind==='群聊'?'group':'direct',contactToken:token,contactId:S(row.entity.id),pinned:false,unread:0,proactiveEnabled:false,createdAt:NOW(),updatedAt:NOW(),messages:[]};s.conversations.push(thread);save()}openThread(thread.id)};
  window.v455PhoneNewTemporaryContact=function(){if(!requireEdit())return;overlay(`${overlayHead('TEMPORARY NPC','新建临时联系人')}<label class="v455-sheet-field"><span>名称</span><input id="v455TempName"></label><label class="v455-sheet-field"><span>关系与依据</span><textarea id="v455TempDesc"></textarea></label>${actions('v455PhoneSaveTemporaryContact()')}`)};
  window.v455PhoneSaveTemporaryContact=function(){const name=document.getElementById('v455TempName')?.value.trim(),description=document.getElementById('v455TempDesc')?.value.trim();if(!name)return toast('请填写名称');store().contacts.push({id:uid('temporary_npc'),name,description,temporary:true,createdAt:NOW()});save();closeOverlay();v455PhoneOpenContacts()};
  window.v455PhoneConvertTemporary=function(id){const s=store(),contact=s.contacts.find(x=>x.id===id&&x.temporary);if(!contact||!confirm(`把“${contact.name}”转为正式 MPC？`))return;const mpc={id:uid('mpc'),name:contact.name,description:contact.description||'',source:'phone-temporary-contact',createdAt:NOW()};data.mpcs=array(data.mpcs);data.mpcs.push(mpc);const oldToken=`temporary:${id}`,newToken=`mpc:${mpc.id}`;s.whitelist=s.whitelist.map(x=>x===oldToken?newToken:x);for(const thread of s.conversations)if(thread.contactToken===oldToken){thread.contactToken=newToken;thread.contactId=mpc.id}s.contacts=s.contacts.filter(x=>x.id!==id);save();v455PhoneOpenContacts();toast('已转为正式 MPC')};

  /* ---------- additive smart completion for My Phone ---------- */
  const PHONE_COMPLETION_SCOPES={messages:'聊天记录',moments:'动态',notes:'便笺',schedule:'日程',browser:'网页收藏',music:'音乐',maps:'地点'};
  function completionModelKind(){if(typeof validModel==='function'&&validModel('personaCompletion'))return'personaCompletion';if(typeof validModel==='function'&&validModel('characterCompletion'))return'characterCompletion';return'chat'}
  function completionAllowed(){return owner==='user'&&session().mode==='browse'}
  function completionContacts(){const allowed=new Set(store().whitelist);return contactCandidates().filter(row=>allowed.has(row.token))}
  function completionContactText(row){
    const entity=row.entity||{},fields=['name','bio','description','personality','background','speechStyle','relationship','scenario'],details=fields.map(key=>S(entity[key]).trim()).filter(Boolean).join('\n').slice(0,1800);let recent='';
    try{const persona=personaFor(),chatId=row.kind==='群聊'?groupChatId(entity.id,persona?.id):row.token.startsWith('character:')?directChatId(entity.id,persona?.id):'';recent=array(data.chats?.[chatId]).filter(message=>message&&message.text&&!message.phoneEvent).slice(-16).map(message=>`${message.role==='user'?(persona?.name||'我'):(message.speakerName||entity.name||'对方')}：${S(message.text)}`).join('\n').slice(0,2600)}catch{}
    return`【${row.token}｜${row.kind}｜${entity.name||'未命名'}】\n${details||'暂无额外资料'}${recent?`\n近期往来：\n${recent}`:''}`
  }
  function phoneExistingSummary(){const s=store(),r=real();return`已有独立会话 ${s.conversations.length} 条；动态 ${r.moments.length} 条；便笺 ${r.notes.length} 条；日程 ${r.schedule.length} 条；网页收藏 ${r.browser.bookmarks.length} 条；音乐 ${r.music.tracks.length} 条；地点 ${r.maps.places.length} 条。\n近期手机内容：\n${[...s.conversations.flatMap(thread=>thread.messages.slice(-3).map(message=>`${thread.title}：${messageText(message)}`)),...r.moments.slice(0,4).map(item=>`动态：${item.text}`),...r.notes.slice(0,4).map(item=>`便笺：${item.title}｜${item.body}`)].slice(-18).join('\n').slice(0,5000)||'暂无'}`}
  function phoneCompletionPicker(){const rows=completionContacts();return`${overlayHead('SMART FILL','完善我的手机')}<p class="v455-sheet-note">默认全选白名单；也可以只留一个，或自由多选。生成结果会先进入预览，不会覆盖现有内容。</p>${rows.length?`<div class="v456-phone-completion-tools"><button onclick="v456PhoneSelectCompletionContacts(true)">全选</button><button onclick="v456PhoneSelectCompletionContacts(false)">清空</button><small>${rows.length} 个白名单联系人</small></div><div class="v456-phone-completion-contacts">${rows.map(row=>`<label><input class="v456-phone-complete-contact" type="checkbox" value="${AT(row.token)}" checked><span>${avatarMarkup(row.entity,row.entity.name,'v455-phone-avatar')}<span><b>${E(row.entity.name||'未命名')}</b><small>${E(row.kind)}</small></span></span></label>`).join('')}</div><div class="v456-phone-completion-scopes"><b>需要完善的页面</b>${Object.entries(PHONE_COMPLETION_SCOPES).map(([key,label])=>`<label><input class="v456-phone-complete-scope" type="checkbox" value="${key}" checked><span>${label}</span></label>`).join('')}</div>${actions('v456PhoneGenerateCompletion()','生成预览')}`:`${empty('联','白名单还是空的','先把至少一个联系人加入白名单，再回来选择。')}<footer><button class="primary" onclick="v455PhoneCloseOverlay()">知道了</button></footer>`}`}
  window.v456PhoneOpenCompletion=function(){if(!completionAllowed())return toast('请从“我的手机”进入智能完善');phoneCompletionDraft=null;overlay(phoneCompletionPicker())};
  window.v456PhoneSelectCompletionContacts=function(checked){document.querySelectorAll('.v456-phone-complete-contact').forEach(input=>input.checked=!!checked)};
  function parsePhoneCompletion(raw){try{if(typeof v436SafeJsonObject==='function')return v436SafeJsonObject(raw);const source=S(raw),start=source.indexOf('{'),end=source.lastIndexOf('}');return JSON.parse(source.slice(start,end+1))}catch{throw Error('返回内容无法识别，请重新生成')}}
  function normalizePhoneCompletion(raw,tokens,scopes){const source=O(raw),allowed=new Set(tokens),use=key=>scopes.includes(key),clean=value=>S(value).replace(/<[^>]+>/g,'').trim();return{
    conversations:use('messages')?array(source.conversations).map(O).filter(row=>allowed.has(S(row.token))).map(row=>({token:S(row.token),messages:array(row.messages).map(O).map(message=>({direction:message.direction==='outgoing'?'outgoing':'incoming',text:clean(message.text)})).filter(message=>message.text).slice(0,8)})).filter(row=>row.messages.length):[],
    moments:use('moments')?array(source.moments).map(O).map(row=>({text:clean(row.text),visibility:clean(row.visibility)||'联系人'})).filter(row=>row.text).slice(0,6):[],
    notes:use('notes')?array(source.notes).map(O).map(row=>({title:clean(row.title),body:clean(row.body)})).filter(row=>row.title&&row.body).slice(0,8):[],
    schedule:use('schedule')?array(source.schedule).map(O).map(row=>({title:clean(row.title),start:clean(row.start),end:clean(row.end),location:clean(row.location),reminder:clean(row.reminder),note:clean(row.note)})).filter(row=>row.title).slice(0,8):[],
    browser:use('browser')?array(source.browser).map(O).map(row=>({title:clean(row.title),url:/^https?:\/\//i.test(clean(row.url))?clean(row.url):'',group:clean(row.group)||'收藏',note:clean(row.note)})).filter(row=>row.title).slice(0,8):[],
    music:use('music')?array(source.music).map(O).map(row=>({title:clean(row.title),artist:clean(row.artist),album:clean(row.album),playlist:clean(row.playlist)||'最近播放'})).filter(row=>row.title).slice(0,8):[],
    maps:use('maps')?array(source.maps).map(O).map(row=>({name:clean(row.name),address:clean(row.address),label:clean(row.label)||'地点',routeFrom:clean(row.routeFrom),transport:clean(row.transport),note:clean(row.note)})).filter(row=>row.name).slice(0,8):[]
  }}
  function completionEntries(draft){return Object.entries(PHONE_COMPLETION_SCOPES).map(([key,label])=>{const list=key==='messages'?draft.conversations:draft[key];return{key,label,list:array(list)}}).filter(group=>group.list.length)}
  function completionPreviewMarkup(){const groups=completionEntries(phoneCompletionDraft||{}),count=groups.reduce((sum,group)=>sum+group.list.length,0);return`${overlayHead('PREVIEW','确认追加内容')}<p class="v455-sheet-note">只会追加下面勾选的项目；已有聊天、动态和其他记录不会被改写。</p><div class="v456-phone-completion-preview">${groups.map(group=>`<section><header><label><input class="v456-phone-complete-result" type="checkbox" value="${group.key}" checked><span><b>${group.label}</b><small>${group.list.length} 项</small></span></label></header>${group.list.slice(0,5).map(item=>`<p>${E(group.key==='messages'?`${completionContacts().find(row=>row.token===item.token)?.entity?.name||'联系人'}：${item.messages.map(message=>message.text).join(' / ')}`:item.text||item.title||item.name||'新内容')}</p>`).join('')}${group.list.length>5?`<small>另有 ${group.list.length-5} 项</small>`:''}</section>`).join('')}</div>${count?actions('v456PhoneApplyCompletion()','追加到我的手机'):`<div class="v455-phone-empty"><span>◇</span><b>没有可靠的新增内容</b><p>现有手机可能已经较完整，也可以换一组联系人重试。</p></div><footer><button onclick="v456PhoneOpenCompletion()">重新选择</button><button class="primary" onclick="v455PhoneCloseOverlay()">完成</button></footer>`}`}
  window.v456PhoneGenerateCompletion=async function(){if(phoneCompletionBusy)return;const tokens=[...document.querySelectorAll('.v456-phone-complete-contact:checked')].map(input=>input.value),scopes=[...document.querySelectorAll('.v456-phone-complete-scope:checked')].map(input=>input.value);if(!tokens.length)return toast('请至少选择一个白名单联系人');if(!scopes.length)return toast('请至少选择一个需要完善的页面');const kind=completionModelKind();if(typeof validModel!=='function'||!validModel(kind))return toast('请先在设置中配置可用的文本模型');const rows=completionContacts().filter(row=>tokens.includes(row.token)),persona=personaFor(),controller=withTimeout(Number(data.settings.timeout)||60000);phoneCompletionBusy=true;setBusy?.(true);toast('正在整理手机内容…');try{const raw=await invokeModel(kind,{system:`你是一名生活资料整理者，要为${persona?.name||'手机主人'}的网站内个人手机补充自然、可信且轻量的生活记录。必须忠于手机主人的既有身份、性格、关系和近期往来；不同联系人必须保持各自说话方式。不要复述近期对话，不要直接续写正在发生的主要事件，而要生成与日常习惯、关系余温、共同兴趣和合理生活细节有关的内容。不得添加重大伤病、犯罪、婚恋、债务、亲属死亡等没有依据的新事实，不得替任何人作重大决定。只追加缺失内容，不改写已有内容。严格只输出 JSON 对象，键为 conversations、moments、notes、schedule、browser、music、maps。conversations 每项为 {token,messages:[{direction:"incoming"或"outgoing",text}]}；其他数组分别使用常见字段。只使用已给出的 token，不输出解释。`,history:[{role:'user',content:`手机主人：${persona?.name||'我'}\n主人资料：\n${['bio','description','personality','background','speechStyle','scenario'].map(key=>S(persona?.[key])).filter(Boolean).join('\n').slice(0,3500)||'暂无额外资料'}\n\n当前时间：${worldTime()}\n需要完善：${scopes.map(key=>PHONE_COMPLETION_SCOPES[key]).join('、')}\n\n选中的联系人：\n${rows.map(completionContactText).join('\n\n').slice(0,18000)}\n\n${phoneExistingSummary()}`}],temperature:.72,maxTokens:4200,signal:controller.signal});phoneCompletionDraft=normalizePhoneCompletion(parsePhoneCompletion(raw),tokens,scopes);overlay(completionPreviewMarkup())}catch(error){errorDetail(error,'手机内容整理失败')}finally{releaseController(controller);phoneCompletionBusy=false;setBusy?.(false)}};
  function appendUnique(list,row,identity){const key=identity(row);if(!key||list.some(item=>identity(item)===key))return false;list.unshift(row);return true}
  window.v456PhoneApplyCompletion=function(){if(!phoneCompletionDraft)return;const selected=new Set([...document.querySelectorAll('.v456-phone-complete-result:checked')].map(input=>input.value));if(!selected.size)return toast('请至少保留一个要追加的页面');const s=store(),r=real(),now=NOW();let added=0;
    if(selected.has('messages'))for(const generated of phoneCompletionDraft.conversations){const row=completionContacts().find(item=>item.token===generated.token);if(!row)continue;let thread=s.conversations.find(item=>item.contactToken===generated.token);if(!thread){thread={id:uid('phone_thread'),title:S(row.entity.name||'未命名会话'),kind:row.kind==='群聊'?'group':'direct',contactToken:generated.token,contactId:S(row.entity.id),pinned:false,unread:0,proactiveEnabled:false,createdAt:now,updatedAt:now,messages:[]};s.conversations.push(thread)}for(const message of generated.messages){if(thread.messages.some(item=>messageText(item).trim()===message.text))continue;const incoming=message.direction==='incoming';thread.messages.push({id:uid('phone_message'),type:'text',direction:incoming?'incoming':'outgoing',text:message.text,localText:message.text,remoteText:message.text,senderId:incoming?S(row.entity.id):'',senderName:incoming?S(row.entity.name):'',actualActor:incoming?S(row.entity.id):'user',displayActor:incoming?S(row.entity.id):'user',worldTimeText:worldTime(),createdAt:now,remoteSynced:true,generatedByCompletion:true,editHistory:[],modifyHistory:[]});added++}thread.updatedAt=now}
    if(selected.has('moments'))for(const item of phoneCompletionDraft.moments)if(appendUnique(r.moments,{id:uid('moment'),...item,images:[],likes:0,liked:false,comments:[],createdAt:now,updatedAt:now,generatedByCompletion:true},row=>S(row.text).trim()))added++;
    if(selected.has('notes'))for(const item of phoneCompletionDraft.notes)if(appendUnique(r.notes,{id:uid('notes'),...item,kind:'纯文本',pinned:'否',createdAt:now,updatedAt:now,generatedByCompletion:true},row=>`${S(row.title).trim()}\n${S(row.body).trim()}`))added++;
    if(selected.has('schedule'))for(const item of phoneCompletionDraft.schedule)if(appendUnique(r.schedule,{id:uid('schedule'),...item,completed:false,createdAt:now,updatedAt:now,generatedByCompletion:true},row=>`${S(row.title).trim()}\n${S(row.start).trim()}`))added++;
    if(selected.has('browser'))for(const item of phoneCompletionDraft.browser)if(appendUnique(r.browser.bookmarks,{id:uid('browser'),...item,createdAt:now,updatedAt:now,generatedByCompletion:true},row=>S(row.url||row.title).trim()))added++;
    if(selected.has('music'))for(const item of phoneCompletionDraft.music)if(appendUnique(r.music.tracks,{id:uid('music'),...item,favorite:'否',createdAt:now,updatedAt:now,generatedByCompletion:true},row=>`${S(row.title).trim()}\n${S(row.artist).trim()}`))added++;
    if(selected.has('maps'))for(const item of phoneCompletionDraft.maps)if(appendUnique(r.maps.places,{id:uid('maps'),...item,createdAt:now,updatedAt:now,generatedByCompletion:true},row=>`${S(row.name).trim()}\n${S(row.address).trim()}`))added++;
    save();phoneCompletionDraft=null;closeOverlay();toast(added?`已追加 ${added} 项手机内容`:'没有发现可新增的内容');openSettings()
  };
  window.v455PhoneOpenMessageMenu=function(messageId){const thread=store().conversations.find(x=>x.id===threadId),message=thread?.messages.find(x=>x.id===messageId);if(!message)return;overlay(`${overlayHead('MESSAGE TOOLS','本机消息操作')}<p class="v455-sheet-note">旧记录的编辑只改变当前手机本地副本；真正发送的新消息才同步远端。</p><div class="v455-sheet-list"><button onclick="v455PhoneEditMessage(${A(messageId)})"><b>静默编辑本机副本</b><small>对方不会凭空知道</small></button><button onclick="v455PhoneShowMessageDiff(${A(messageId)})"><b>本机 / 远端差异</b><small>查看原始远端记录</small></button><button class="danger" onclick="v455PhoneDeleteMessage(${A(messageId)})"><b>删除本机消息</b><small>移入当前手机回收站</small></button></div>${actions('v455PhoneCloseOverlay()','完成')}`)};
  window.v455PhoneEditMessage=function(messageId){if(!requireEdit())return;const message=store().conversations.find(x=>x.id===threadId)?.messages.find(x=>x.id===messageId);if(!message)return;overlay(`${overlayHead('EDIT LOCAL COPY','编辑本机副本')}<label class="v455-sheet-field"><span>本机内容</span><textarea id="v455EditPhoneMessage">${E(messageText(message))}</textarea></label>${actions(`v455PhoneSaveMessageEdit(${A(messageId)})`)}`)};
  window.v455PhoneSaveMessageEdit=function(messageId){const thread=store().conversations.find(x=>x.id===threadId),message=thread?.messages.find(x=>x.id===messageId),text=document.getElementById('v455EditPhoneMessage')?.value.trim();if(!message||!text)return toast('内容不能为空');message.editHistory=array(message.editHistory);message.editHistory.push({before:messageText(message),after:text,actualActor:actualActor(session()),at:NOW()});message.localText=text;message.editedAt=NOW();save();closeOverlay();openThread(thread.id)};
  window.v455PhoneShowMessageDiff=function(messageId){const message=store().conversations.find(x=>x.id===threadId)?.messages.find(x=>x.id===messageId);if(!message)return;overlay(`${overlayHead('LOCAL / REMOTE','本机 / 远端差异')}<div class="v455-diff-card"><b>本机副本</b><p>${E(messageText(message))}</p><b>远端原始记录</b><p>${E(message.remoteText||message.text||'')}</p><small>实际操作者保存在隐藏记录 · ${E(message.editedAt||message.createdAt||'')}</small></div>${actions('v455PhoneCloseOverlay()','完成')}`)};
  window.v455PhoneDeleteMessage=function(messageId){if(!requireEdit()||!confirm('删除这条本机消息并移入回收站？'))return;const s=store(),thread=s.conversations.find(x=>x.id===threadId),index=thread?.messages.findIndex(x=>x.id===messageId);if(index<0)return;const message=thread.messages[index];s.trash.push({id:uid('phone_trash'),kind:'message',snapshot:clone(message),original:{app:'messages',threadId:thread.id,index},deleteSemantics:deleteMode,deletedAt:NOW()});thread.messages.splice(index,1);recordOperation('message-delete',{localChanged:true,remoteSynced:false,summary:`删除${thread.title}中的本机消息`},deleteMode==='plot');save();closeOverlay();openThread(thread.id)};
  window.v455PhoneOpenMessageManagement=function(){const s=store(),thread=s.conversations.find(x=>x.id===threadId);selectedMessages=new Set();if(thread){const rows=thread.messages.map(m=>`<label class="v455-select-message"><input type="checkbox" onchange="v455PhoneToggleMessageSelection(${A(m.id)},this.checked)"><span><b>${E(messageText(m).slice(0,90)||'空消息')}</b><small>${E(m.worldTimeText||displayTime(m.createdAt))}</small></span><i>${m.direction==='incoming'?'收到':'发出'}</i></label>`).join('');shell('聊天记录管理',`${rows?`<div class="v455-select-list">${rows}</div>`:empty('删','当前会话没有消息','')}<div class="v455-delete-semantics"><label><input type="radio" name="v455DeleteMode" value="silent" ${deleteMode==='silent'?'checked':''} onchange="deleteMode='silent'"><span><b>静默删除</b><small>只改本机，对方不会凭空知道</small></span></label><label><input type="radio" name="v455DeleteMode" value="plot" ${deleteMode==='plot'?'checked':''} onchange="deleteMode='plot'"><span><b>双方会记得</b><small>写入相关手机事件记忆</small></span></label></div><div class="v455-app-actions"><button class="danger" onclick="v455PhoneDeleteSelectedMessages()">删除所选</button><button onclick="v455PhoneOpenTrash()">回收站</button></div>`,{backAction:`v455PhoneOpenThread(${A(thread.id)})`,right:'完成',rightAction:`v455PhoneOpenThread(${A(thread.id)})`});return}const count=s.conversations.reduce((n,t)=>n+t.messages.length,0);shell('聊天管理',`<div class="v455-management-grid"><button onclick="v455PhoneOpenContacts()"><span>联</span><b>联系人白名单</b><small>${s.whitelist.length} 个可联系对象</small></button><button onclick="v455PhoneOpenTrash()"><span>删</span><b>回收站</b><small>${s.trash.length} 项</small></button><button onclick="v455PhoneClearMessages()"><span>清</span><b>清空聊天应用</b><small>${count} 条消息</small></button><button onclick="v455PhoneReset()"><span>重</span><b>重置整部手机</b><small>精确选择范围</small></button></div>`,{backAction:'v455PhoneOpenMessages()',right:'完成',rightAction:'v455PhoneOpenMessages()'})};
  window.v455PhoneToggleMessageSelection=function(id,checked){checked?selectedMessages.add(id):selectedMessages.delete(id)};
  window.v455PhoneDeleteSelectedMessages=function(){if(!requireEdit()||!selectedMessages.size)return toast('请先选择消息');if(!confirm(`删除所选 ${selectedMessages.size} 条本机消息？`))return;const s=store(),thread=s.conversations.find(x=>x.id===threadId);for(let i=thread.messages.length-1;i>=0;i--){const message=thread.messages[i];if(!selectedMessages.has(message.id))continue;s.trash.push({id:uid('phone_trash'),kind:'message',snapshot:clone(message),original:{app:'messages',threadId:thread.id,index:i},deleteSemantics:deleteMode,deletedAt:NOW()});thread.messages.splice(i,1)}recordOperation('message-delete-many',{localChanged:true,remoteSynced:false,summary:`多选删除 ${selectedMessages.size} 条本机消息`},deleteMode==='plot');save();v455PhoneOpenMessageManagement()};
  window.v455PhoneClearMessages=function(){if(!requireEdit()||!confirm('清空当前手机的聊天应用？会话将进入本机回收站，另一部手机和普通聊天不受影响。'))return;const s=store();for(const t of s.conversations)s.trash.push({id:uid('phone_trash'),kind:'conversation',snapshot:clone(t),original:{app:'messages'},deletedAt:NOW(),expiresAt:s.phoneSettings.trashRetention==='permanent'?'':new Date(Date.now()+30*86400000).toISOString()});s.conversations=[];save();recordOperation('app-clear',{localChanged:true,summary:'清空聊天应用'},true);v455PhoneOpenMessageManagement()};
  window.v455PhoneOpenTrash=function(){const s=store(),rows=s.trash.slice().reverse().map(x=>`<article class="v455-trash-row"><span>⌁</span><div><b>${E(x.snapshot?.title||x.snapshot?.product||x.snapshot?.localText||x.snapshot?.text||'已删除项目')}</b><small>${E(x.deletedAt||'')}</small></div><button onclick="v455PhoneRestoreTrash(${A(x.id)})">恢复</button></article>`).join('');shell('回收站',`${rows?`<div class="v455-trash-list">${rows}</div>`:empty('⌁','回收站为空','删除内容会进入当前手机自己的回收站。')}<div class="v455-app-actions"><button class="danger" onclick="v455PhoneEmptyTrash()">清空回收站</button></div>`,{backAction:'v455PhoneOpenMessageManagement()',right:'设置',rightAction:'v455PhoneTrashSettings()'})};
  window.v455PhoneRestoreTrash=function(id){const s=store(),i=s.trash.findIndex(x=>x.id===id),entry=s.trash[i];if(!entry)return;if(entry.kind==='conversation')s.conversations.push(entry.snapshot);else if(entry.kind==='message'){let thread=s.conversations.find(x=>x.id===entry.original?.threadId);if(!thread){thread={id:S(entry.original?.threadId||uid('phone_thread')),title:'恢复的会话',kind:'direct',messages:[],createdAt:NOW(),updatedAt:NOW()};s.conversations.push(thread)}thread.messages.splice(Math.min(thread.messages.length,Math.max(0,Number(entry.original?.index)||0)),0,entry.snapshot)}else if(entry.kind==='photo')s.realAppsV455.gallery.photos.splice(Math.min(s.realAppsV455.gallery.photos.length,Math.max(0,Number(entry.original?.index)||0)),0,entry.snapshot);else if(entry.kind==='real-app'&&schemas[entry.original?.app])schemas[entry.original.app].list(s.realAppsV455).splice(Math.min(schemas[entry.original.app].list(s.realAppsV455).length,Math.max(0,Number(entry.original?.index)||0)),0,entry.snapshot);else if(entry.kind==='moment')s.realAppsV455.moments.splice(Math.min(s.realAppsV455.moments.length,Math.max(0,Number(entry.original?.index)||0)),0,entry.snapshot);else if(entry.kind==='transaction')s.realAppsV455.wallet.transactions.splice(Math.min(s.realAppsV455.wallet.transactions.length,Math.max(0,Number(entry.original?.index)||0)),0,entry.snapshot);s.trash.splice(i,1);save();v455PhoneOpenTrash()};window.v455PhoneEmptyTrash=function(){if(!requireEdit()||!confirm('永久清空当前手机回收站？'))return;store().trash=[];save();v455PhoneOpenTrash()};
  window.v455PhoneTrashSettings=function(){overlay(`${overlayHead('TRASH SETTINGS','回收站保留期')}<label class="v455-sheet-field"><span>保留规则</span><select id="v455TrashRetention"><option value="30" ${store().phoneSettings.trashRetention!=='permanent'?'selected':''}>30 天</option><option value="permanent" ${store().phoneSettings.trashRetention==='permanent'?'selected':''}>永久</option></select></label>${actions('v455PhoneSaveTrashSettings()')}`)};window.v455PhoneSaveTrashSettings=function(){store().phoneSettings.trashRetention=document.getElementById('v455TrashRetention')?.value==='permanent'?'permanent':30;save();closeOverlay();v455PhoneOpenTrash()};
  window.v455PhoneReset=function(){if(!requireEdit())return;const personaName=personaFor()?.name||'当前面具';overlay(`${overlayHead('RESET PHONE','重置整部手机')}<label class="v455-sheet-radio"><input type="radio" name="v455Reset" value="content" checked><span><b>只清内容</b><small>保留联系人、白名单、外观和设置</small></span></label><label class="v455-sheet-radio"><input type="radio" name="v455Reset" value="all"><span><b>内容与本机设置一起重置</b><small>清空内容、联系人、白名单和本机设置</small></span></label><p class="v455-sheet-note">绝不会删除普通聊天、人物、${E(personaName)}、世界书、记忆或另一部手机。</p>${actions('v455PhoneConfirmReset()','确认重置')}`)};window.v455PhoneConfirmReset=function(){if(!confirm('确认按所选范围重置当前手机？'))return;const s=store(),mode=document.querySelector('input[name="v455Reset"]:checked')?.value||'content';s.items=[];s.conversations=[];s.realAppsV455=null;V.ensureRealApps(s,owner);if(mode==='all'){s.contacts=[];s.whitelist=[];s.phoneSettings={trashRetention:30,wallpaper:'',appearance:'dark'}}save();closeOverlay();v43PhoneDesktop(owner);toast('当前手机已按精确范围重置')};
  window.v455PhoneOpenDiffList=function(){const thread=store().conversations.find(x=>x.id===threadId),rows=(thread?.messages||[]).filter(m=>S(m.localText)!==S(m.remoteText)).map(m=>`<article class="v455-diff-card"><b>本机副本</b><p>${E(m.localText)}</p><b>远端记录</b><p>${E(m.remoteText)}</p></article>`).join('');shell('本机 / 远端差异',rows||empty('差','当前没有差异','静默编辑旧记录后会在这里显示。'),{backAction:`v455PhoneOpenThread(${A(threadId)})`})};

  /* ---------- Moments: feed/media/likes/comments ---------- */
  function momentRows(){return real().moments.slice().sort((a,b)=>S(b.createdAt).localeCompare(S(a.createdAt)))}
  function mediaMarkup(images=[]){const valid=images.map(imageSource).filter(Boolean);return valid.length?`<div class="v455-moment-media count-${Math.min(valid.length,4)}">${valid.slice(0,9).map(src=>`<img src="${AT(src)}" alt="">`).join('')}</div>`:''}
  function momentCard(post){const comments=array(post.comments);return`<article class="v455-moment-card" onclick="v455PhoneOpenMoment(${A(post.id)})"><header>${ownerAvatar('v455-phone-avatar')}<div><b>${E(ownerName(owner))}</b><small>${E(displayTime(post.createdAt)||post.worldTimeText||'')}</small></div><button onclick="event.stopPropagation();v455PhoneEditMoment(${A(post.id)})">•••</button></header><p>${E(post.text||'')}</p>${mediaMarkup(post.images)}<footer><button class="${post.liked?'on':''}" onclick="event.stopPropagation();v455PhoneLikeMoment(${A(post.id)})">${post.liked?'♥':'♡'} ${Math.max(0,Number(post.likes)||0)}</button><button>◇ ${comments.length}</button></footer>${comments.length?`<div class="v455-moment-comments"><b>${E(comments[0].author||'联系人')}：</b>${E(comments[0].text||'')}</div>`:''}</article>`}
  function openMoments(){app='moments';const rows=momentRows(),body=`${reactionMarkup('moments',`${rows.length} 条动态`)}<div class="v455-phone-toolbar"><span>${rows.length} 条动态</span>${allowEdit()?'<button onclick="v455PhoneNewMoment()">＋ 发布</button>':''}</div><section class="v452-moment-cover"><small>${E(ownerName(owner).toUpperCase())} · MOMENTS</small><h3>${E(ownerName(owner))}的动态</h3><p>正文、媒体、可见范围、点赞和评论保存在这部手机中。</p></section>${rows?`<div class="v455-moment-feed">${rows.map(momentCard).join('')}</div>`:empty('◌','还没有动态','发布的正文、图片、点赞和评论会显示在这里。',allowEdit()?'v455PhoneNewMoment()':'','发布动态')}`;shell('动态',body)}
  window.v455PhoneOpenMoments=openMoments;
  window.v455PhoneLikeMoment=function(id){const p=real().moments.find(x=>x.id===id);if(!p)return;p.liked=!p.liked;p.likes=Math.max(0,Number(p.likes)||0)+(p.liked?1:-1);save();postId===id?v455PhoneOpenMoment(id):openMoments()};
  window.v455PhoneOpenMoment=function(id){const p=real().moments.find(x=>x.id===id);if(!p)return;postId=id;const comments=array(p.comments).map(c=>`<article class="v455-comment-row">${avatarMarkup(entityFromToken(c.authorToken),c.author||'联','v455-phone-avatar')}<div><b>${E(c.author||'联系人')}</b><p>${E(c.text)}</p><small>${E(displayTime(c.createdAt))}</small></div></article>`).join('');shell('动态详情',`${reactionMarkup(`moment:${id}`,p.text)}${momentCard(p)}<div class="v455-section-title"><b>评论</b><small>${array(p.comments).length}</small></div>${comments||empty('◇','还没有评论','可以添加第一条评论。')}<div class="v455-app-actions"><button onclick="v455PhoneCommentMoment(${A(id)})">＋ 评论</button>${allowEdit()?`<button onclick="v455PhoneEditMoment(${A(id)})">编辑动态</button>`:''}</div>`,{backAction:'v455PhoneOpenMoments()'})};
  window.v455PhoneNewMoment=()=>editMoment('');window.v455PhoneEditMoment=id=>editMoment(id);
  function editMoment(id){if(!requireEdit())return;const p=real().moments.find(x=>x.id===id)||{text:'',images:[],visibility:'联系人'};overlay(`${overlayHead(id?'EDIT MOMENT':'NEW MOMENT',id?'编辑动态':'发布动态')}<label class="v455-sheet-field"><span>正文</span><textarea id="v455MomentText">${E(p.text)}</textarea></label><label class="v455-sheet-field"><span>图片 URL（每行一个，也可随后上传）</span><textarea id="v455MomentImages">${E(array(p.images).join('\n'))}</textarea></label><label class="v455-sheet-field"><span>可见范围</span><select id="v455MomentVisibility"><option ${p.visibility==='联系人'?'selected':''}>联系人</option><option ${p.visibility==='仅自己'?'selected':''}>仅自己</option><option ${p.visibility==='自定义'?'selected':''}>自定义</option></select></label><button class="v455-wide-button" onclick="v455PhoneImportMomentImage()">从本机选择图片</button><div id="v455MomentImported"></div>${actions(`v455PhoneSaveMoment(${A(id)})`,'保存',id?`<button class="danger" onclick="v455PhoneDeleteMoment(${A(id)})">删除</button>`:'')}`)}
  window.v455PhoneImportMomentImage=function(){const input=document.createElement('input');input.type='file';input.accept='image/*';input.multiple=true;input.onchange=async()=>{const images=[];for(const file of input.files||[])try{images.push(await readImageFile(file))}catch{}const target=document.getElementById('v455MomentImported');if(target){target.dataset.images=JSON.stringify(images);target.textContent=`已选择 ${images.length} 张图片`}};input.click()};
  window.v455PhoneSaveMoment=function(id){const text=document.getElementById('v455MomentText')?.value.trim();if(!text)return toast('请填写动态正文');const imported=JSON.parse(document.getElementById('v455MomentImported')?.dataset.images||'[]'),urls=S(document.getElementById('v455MomentImages')?.value).split(/\n+/).map(x=>x.trim()).filter(Boolean),p=real().moments.find(x=>x.id===id);if(p){p.text=text;p.images=[...urls,...imported];p.visibility=document.getElementById('v455MomentVisibility')?.value;p.updatedAt=NOW()}else real().moments.unshift({id:uid('moment'),text,images:[...urls,...imported],visibility:document.getElementById('v455MomentVisibility')?.value||'联系人',likes:0,liked:false,comments:[],createdAt:NOW(),worldTimeText:worldTime()});save();closeOverlay();id?v455PhoneOpenMoment(id):openMoments()};
  window.v455PhoneDeleteMoment=function(id){if(!confirm('删除这条动态并移入当前手机回收站？'))return;const list=real().moments,i=list.findIndex(x=>x.id===id);if(i<0)return;store().trash.push({id:uid('phone_trash'),kind:'moment',snapshot:clone(list[i]),original:{app:'moments',index:i},deletedAt:NOW()});list.splice(i,1);save();closeOverlay();openMoments()};
  window.v455PhoneCommentMoment=function(id){overlay(`${overlayHead('COMMENT','添加评论')}<label class="v455-sheet-field"><span>评论</span><textarea id="v455MomentComment"></textarea></label>${actions(`v455PhoneSaveMomentComment(${A(id)})`)}`)};window.v455PhoneSaveMomentComment=function(id){const text=document.getElementById('v455MomentComment')?.value.trim();if(!text)return toast('请输入评论');const p=real().moments.find(x=>x.id===id);p.comments=array(p.comments);p.comments.push({id:uid('comment'),author:ownerName(owner),authorToken:owner==='user'?`persona:${personaFor()?.id}`:`character:${owner}`,text,createdAt:NOW(),likes:0});save();closeOverlay();v455PhoneOpenMoment(id)};

  /* ---------- Gallery: albums/grid/photo detail ---------- */
  function openGallery(){app='gallery';const photos=real().gallery.photos,albums=real().gallery.albums,content=galleryTab==='albums'?`<div class="v455-album-list"><button onclick="galleryTab='photos';v455PhoneOpenGallery()"><span>${photos[0]?.src?`<img src="${AT(imageSource(photos[0].src))}" alt="">`:'▧'}</span><b>最近项目</b><small>${photos.length} 张</small></button>${albums.map(a=>`<button onclick="v455PhoneOpenAlbum(${A(a.id)})"><span>${a.cover?`<img src="${AT(imageSource(a.cover))}" alt="">`:'▧'}</span><b>${E(a.name)}</b><small>${photos.filter(p=>p.albumId===a.id).length} 张</small></button>`).join('')}</div>`:photoGrid(photos);shell('相册',`${reactionMarkup('gallery',`${photos.length} 张照片`)}<div class="v455-phone-toolbar"><span>${photos.length} 项</span>${allowEdit()?'<button onclick="v455PhoneNewPhoto()">＋ 导入</button>':''}</div><section class="v452-gallery-head"><small>ALBUM</small><h3>${photos.length} 项影像记录</h3><p>相册、缩略图、照片详情、时间、地点和说明均可管理。</p></section><div class="v455-gallery-tabs"><button class="${galleryTab==='photos'?'on':''}" onclick="galleryTab='photos';v455PhoneOpenGallery()">照片</button><button class="${galleryTab==='albums'?'on':''}" onclick="galleryTab='albums';v455PhoneOpenGallery()">相册</button><button onclick="v455PhoneOpenTrash()">最近删除</button></div>${content}`)}
  window.v455PhoneOpenGallery=openGallery;
  function photoGrid(photos){return photos.length?`<div class="v455-gallery-grid">${photos.map(p=>`<button onclick="v455PhoneOpenPhoto(${A(p.id)})">${imageSource(p.src)?`<img src="${AT(imageSource(p.src))}" alt="">`:'<span>▧</span>'}<small>${E(p.title||'照片')}</small></button>`).join('')}</div>`:empty('▧','还没有照片','可导入本机图片或使用图片 URL。',allowEdit()?'v455PhoneNewPhoto()':'','导入照片')}
  window.v455PhoneOpenAlbum=function(id){const album=real().gallery.albums.find(x=>x.id===id);if(!album)return;const photos=real().gallery.photos.filter(p=>p.albumId===id);shell(album.name,photoGrid(photos),{backAction:'v455PhoneOpenGallery()',right:allowEdit()?'编辑':'',rightAction:allowEdit()?`v455PhoneEditAlbum(${A(id)})`:''})};
  window.v455PhoneOpenPhoto=function(id){const p=real().gallery.photos.find(x=>x.id===id);if(!p)return;photoId=id;shell('照片详情',`${reactionMarkup(`photo:${id}`,p.note||p.title)}<div class="v455-photo-large">${imageSource(p.src)?`<img src="${AT(imageSource(p.src))}" alt="">`:'<span>▧</span>'}</div><article class="v455-photo-info"><h2>${E(p.title||'未命名照片')}</h2><p>${E(p.takenAt||displayTime(p.createdAt))}<br>${E(p.place||'未填写地点')}<br>${E(p.note||'未填写说明')}</p></article>${allowEdit()?`<div class="v455-app-actions"><button onclick="v455PhoneEditPhoto(${A(id)})">编辑信息</button><button class="danger" onclick="v455PhoneDeletePhoto(${A(id)})">删除照片</button></div>`:''}`,{backAction:'v455PhoneOpenGallery()'})};
  window.v455PhoneNewPhoto=()=>editPhoto('');window.v455PhoneEditPhoto=id=>editPhoto(id);
  function editPhoto(id){if(!requireEdit())return;const p=real().gallery.photos.find(x=>x.id===id)||{title:'',src:'',albumId:'recent',takenAt:'',place:'',note:''};overlay(`${overlayHead(id?'EDIT PHOTO':'IMPORT PHOTO',id?'编辑照片信息':'导入照片')}<label class="v455-sheet-field"><span>标题</span><input id="v455PhotoTitle" value="${AT(p.title)}"></label><label class="v455-sheet-field"><span>图片 URL</span><input id="v455PhotoSrc" value="${AT(p.src)}"></label><button class="v455-wide-button" onclick="v455PhonePickPhotoFile()">从本机选择图片</button><div id="v455PickedPhoto"></div><label class="v455-sheet-field"><span>相册</span><select id="v455PhotoAlbum"><option value="recent">最近项目</option>${real().gallery.albums.map(a=>`<option value="${AT(a.id)}" ${p.albumId===a.id?'selected':''}>${E(a.name)}</option>`).join('')}</select></label><label class="v455-sheet-field"><span>拍摄时间 / 世界时间</span><input id="v455PhotoTime" value="${AT(p.takenAt)}"></label><label class="v455-sheet-field"><span>地点</span><input id="v455PhotoPlace" value="${AT(p.place)}"></label><label class="v455-sheet-field"><span>说明</span><textarea id="v455PhotoNote">${E(p.note)}</textarea></label>${actions(`v455PhoneSavePhoto(${A(id)})`)}`)}
  window.v455PhonePickPhotoFile=function(){const input=document.createElement('input');input.type='file';input.accept='image/*';input.onchange=async()=>{const file=input.files?.[0];if(!file)return;try{const src=await readImageFile(file),target=document.getElementById('v455PickedPhoto');target.dataset.src=src;target.textContent=`已选择：${file.name}`}catch(error){errorDetail(error,'图片读取失败')}};input.click()};
  window.v455PhoneSavePhoto=function(id){const picked=document.getElementById('v455PickedPhoto')?.dataset.src||'',src=picked||document.getElementById('v455PhotoSrc')?.value.trim(),title=document.getElementById('v455PhotoTitle')?.value.trim()||'未命名照片';if(!src)return toast('请选择图片或填写图片 URL');const p=real().gallery.photos.find(x=>x.id===id),row={title,src,albumId:document.getElementById('v455PhotoAlbum')?.value||'recent',takenAt:document.getElementById('v455PhotoTime')?.value.trim()||worldTime(),place:document.getElementById('v455PhotoPlace')?.value.trim()||'',note:document.getElementById('v455PhotoNote')?.value.trim()||'',updatedAt:NOW()};if(p)Object.assign(p,row);else real().gallery.photos.unshift({id:uid('photo'),...row,createdAt:NOW()});save();closeOverlay();id?v455PhoneOpenPhoto(id):openGallery()};
  window.v455PhoneDeletePhoto=function(id){if(!confirm('把这张照片移入当前手机回收站？'))return;const r=real(),i=r.gallery.photos.findIndex(x=>x.id===id),p=r.gallery.photos[i];if(!p)return;store().trash.push({id:uid('phone_trash'),kind:'photo',snapshot:clone(p),original:{app:'gallery',index:i},deletedAt:NOW()});r.gallery.photos.splice(i,1);save();openGallery()};
  window.v455PhoneEditAlbum=function(id){if(!requireEdit())return;const a=real().gallery.albums.find(x=>x.id===id);overlay(`${overlayHead('EDIT ALBUM','编辑相册')}<label class="v455-sheet-field"><span>名称</span><input id="v455AlbumName" value="${AT(a.name)}"></label>${actions(`v455PhoneSaveAlbum(${A(id)})`)}`)};window.v455PhoneSaveAlbum=function(id){const a=real().gallery.albums.find(x=>x.id===id),name=document.getElementById('v455AlbumName')?.value.trim();if(!name)return toast('请输入名称');a.name=name;save();closeOverlay();v455PhoneOpenAlbum(id)};

  /* ---------- Wallet: account/balance/income/expense/bills ---------- */
  function activeAccount(){return real().wallet.accounts[0]||null}
  function openWallet(){app='wallet';const account=activeAccount(),transactions=real().wallet.transactions.filter(t=>walletFilter==='all'||t.direction===walletFilter||walletFilter==='bill'&&t.kind==='bill'),card=account?`<section class="v455-wallet-card"><small>${E(account.name||`${ownerName(owner)} · 虚构账户`)}</small><strong>${E(account.currency==='USD'?'$':'¥')} ${Number(account.balance||0).toLocaleString('zh-CN',{minimumFractionDigits:2})}</strong><p>${account.last4?`尾号 ${E(account.last4)}`:'未设置尾号'}　仅为扑克机内部虚构资料</p></section>`:empty('◈','还没有虚构账户','先新建账户，再设置余额和记录收支。',allowEdit()?'v455PhoneEditAccount()':'','新建账户');shell('银行卡',`${reactionMarkup('wallet',account?`余额 ${account.balance}`:'暂无账户')}<div class="v455-phone-toolbar"><span>${real().wallet.accounts.length} 个账户</span>${allowEdit()?'<button onclick="v455PhoneEditAccount()">账户管理</button>':''}</div>${card}${account&&allowEdit()?`<div class="v455-wallet-actions"><button onclick="v455PhoneSetBalance()">设置余额</button><button onclick="v455PhoneNewTransaction('income')">记收入</button><button onclick="v455PhoneNewTransaction('expense')">记支出</button><button onclick="v455PhoneNewTransaction('bill')">添加账单</button></div>`:''}<div class="v455-wallet-tabs"><button class="${walletFilter==='all'?'on':''}" onclick="walletFilter='all';v455PhoneOpenWallet()">全部</button><button class="${walletFilter==='income'?'on':''}" onclick="walletFilter='income';v455PhoneOpenWallet()">收入</button><button class="${walletFilter==='expense'?'on':''}" onclick="walletFilter='expense';v455PhoneOpenWallet()">支出</button><button class="${walletFilter==='bill'?'on':''}" onclick="walletFilter='bill';v455PhoneOpenWallet()">账单</button></div>${transactions.length?`<div class="v455-transaction-list">${transactions.map(t=>`<button onclick="v455PhoneEditTransaction(${A(t.id)})"><span>${t.direction==='income'?'入':'出'}</span><div><b>${E(t.title||t.category||'账目')}</b><small>${E(t.worldTimeText||displayTime(t.createdAt))} · ${E(t.category||'未分类')}</small></div><em class="${t.direction}">${t.direction==='income'?'+':'−'} ${Number(t.amount||0).toLocaleString('zh-CN',{minimumFractionDigits:2})}</em></button>`).join('')}</div>`:empty('◈','当前分类没有账目','收入、支出与账单会真实联动账户余额。')}`)}
  window.v455PhoneOpenWallet=openWallet;
  window.v455PhoneEditAccount=function(){if(!requireEdit())return;const a=activeAccount()||{name:`${ownerName(owner)} · 虚构账户`,last4:'',currency:'CNY',balance:0};overlay(`${overlayHead(a.id?'EDIT ACCOUNT':'NEW ACCOUNT',a.id?'编辑虚构账户':'新建虚构账户')}<label class="v455-sheet-field"><span>账户名称</span><input id="v455AccountName" value="${AT(a.name)}"></label><label class="v455-sheet-field"><span>尾号</span><input id="v455AccountLast4" value="${AT(a.last4)}" maxlength="4"></label><label class="v455-sheet-field"><span>币种</span><select id="v455AccountCurrency"><option value="CNY" ${a.currency!=='USD'?'selected':''}>CNY · ¥</option><option value="USD" ${a.currency==='USD'?'selected':''}>USD · $</option></select></label><label class="v455-sheet-field"><span>初始余额</span><input id="v455AccountBalance" type="number" value="${Number(a.balance)||0}"></label>${actions(`v455PhoneSaveAccount(${A(a.id||'')})`)}`)};window.v455PhoneSaveAccount=function(id){const name=document.getElementById('v455AccountName')?.value.trim(),balance=Number(document.getElementById('v455AccountBalance')?.value);if(!name||!Number.isFinite(balance))return toast('请填写有效账户和余额');const row={name,last4:document.getElementById('v455AccountLast4')?.value.trim(),currency:document.getElementById('v455AccountCurrency')?.value||'CNY',balance,updatedAt:NOW()},a=real().wallet.accounts.find(x=>x.id===id);if(a)Object.assign(a,row);else real().wallet.accounts.unshift({id:uid('account'),...row,createdAt:NOW()});save();closeOverlay();openWallet()};
  window.v455PhoneSetBalance=function(){const a=activeAccount();if(!a||!requireEdit())return;overlay(`${overlayHead('SET BALANCE','设置余额')}<label class="v455-sheet-field"><span>新的余额</span><input id="v455BalanceValue" type="number" value="${Number(a.balance)||0}"></label><p class="v455-sheet-note">直接校准虚构账户余额；不会连接现实银行卡。</p>${actions('v455PhoneSaveBalance()')}`)};window.v455PhoneSaveBalance=function(){const a=activeAccount(),value=Number(document.getElementById('v455BalanceValue')?.value);if(!Number.isFinite(value))return toast('请输入有效余额');a.balance=value;a.updatedAt=NOW();save();closeOverlay();openWallet()};
  window.v455PhoneNewTransaction=kind=>editTransaction('',kind);window.v455PhoneEditTransaction=id=>editTransaction(id,'');
  function editTransaction(id,kind){if(!requireEdit())return;const t=real().wallet.transactions.find(x=>x.id===id)||{title:'',amount:0,direction:kind==='income'?'income':'expense',kind:kind==='bill'?'bill':'transaction',category:kind==='bill'?'账单':'',counterparty:'',worldTimeText:worldTime()};overlay(`${overlayHead(id?'EDIT TRANSACTION':'NEW TRANSACTION',id?'编辑账目':t.kind==='bill'?'添加账单':t.direction==='income'?'记录收入':'记录支出')}<label class="v455-sheet-field"><span>名称</span><input id="v455TxTitle" value="${AT(t.title)}"></label><label class="v455-sheet-field"><span>金额</span><input id="v455TxAmount" type="number" value="${Number(t.amount)||0}"></label><label class="v455-sheet-field"><span>方向</span><select id="v455TxDirection"><option value="income" ${t.direction==='income'?'selected':''}>收入</option><option value="expense" ${t.direction!=='income'?'selected':''}>支出</option></select></label><label class="v455-sheet-field"><span>分类</span><input id="v455TxCategory" value="${AT(t.category)}"></label><label class="v455-sheet-field"><span>对方 / 商户</span><input id="v455TxCounterparty" value="${AT(t.counterparty)}"></label><label class="v455-sheet-field"><span>故事时间</span><input id="v455TxTime" value="${AT(t.worldTimeText)}"></label>${actions(`v455PhoneSaveTransaction(${A(id)},${A(t.kind)})`,'保存',id?`<button class="danger" onclick="v455PhoneDeleteTransaction(${A(id)})">删除</button>`:'')}`)}
  window.v455PhoneSaveTransaction=function(id,kind){const amount=Number(document.getElementById('v455TxAmount')?.value),title=document.getElementById('v455TxTitle')?.value.trim();if(!title||!Number.isFinite(amount)||amount<0)return toast('请填写有效名称和金额');const direction=document.getElementById('v455TxDirection')?.value==='income'?'income':'expense',row={title,amount,direction,kind:S(kind||'transaction'),category:document.getElementById('v455TxCategory')?.value.trim(),counterparty:document.getElementById('v455TxCounterparty')?.value.trim(),worldTimeText:document.getElementById('v455TxTime')?.value.trim()||worldTime(),updatedAt:NOW()},list=real().wallet.transactions,t=list.find(x=>x.id===id),account=activeAccount();if(t&&account)account.balance-=t.direction==='income'?Number(t.amount)||0:-(Number(t.amount)||0);if(t)Object.assign(t,row);else list.unshift({id:uid('transaction'),...row,createdAt:NOW()});if(account)account.balance+=direction==='income'?amount:-amount;save();closeOverlay();openWallet()};
  window.v455PhoneDeleteTransaction=function(id){if(!confirm('删除这条账目并移入回收站？'))return;const list=real().wallet.transactions,i=list.findIndex(x=>x.id===id),t=list[i],a=activeAccount();if(t&&a)a.balance-=t.direction==='income'?Number(t.amount)||0:-(Number(t.amount)||0);if(i>=0){store().trash.push({id:uid('phone_trash'),kind:'transaction',snapshot:clone(t),original:{app:'wallet',index:i},deletedAt:NOW()});list.splice(i,1)}save();closeOverlay();openWallet()};

  /* ---------- Remaining apps: app-specific schemas and layouts ---------- */
  const schemas={
    notes:{list:r=>r.notes,fields:[['title','标题','text'],['kind','类型','select',['纯文本','待办清单']],['body','正文 / 清单项','textarea'],['pinned','置顶','select',['否','是']]],title:x=>x.title,subtitle:x=>x.body},
    market:{list:r=>r.market.orders,fields:[['product','商品名称','text'],['amount','金额','number'],['quantity','数量','number'],['status','订单状态','select',['待付款','运输中','已完成','退款']],['logistics','物流信息','textarea'],['address','收件信息','textarea']],title:x=>x.product,subtitle:x=>`${x.status||'订单'} · ¥${Number(x.amount||0).toFixed(2)} · ${x.logistics||''}`},
    browser:{list:r=>r.browser.bookmarks,fields:[['title','网页名称','text'],['url','网址','url'],['group','分组','text'],['note','备注 / 阅读状态','textarea']],title:x=>x.title,subtitle:x=>`${x.url||''} · ${x.group||'书签'}`},
    schedule:{list:r=>r.schedule,fields:[['title','日程标题','text'],['start','开始时间','text'],['end','结束时间','text'],['location','地点','text'],['reminder','提醒规则','text'],['note','备注','textarea']],title:x=>x.title,subtitle:x=>`${x.start||''} · ${x.location||''}`},
    music:{list:r=>r.music.tracks,fields:[['title','歌曲名称','text'],['artist','歌手','text'],['album','专辑','text'],['playlist','播放列表','text'],['favorite','收藏','select',['否','是']]],title:x=>x.title,subtitle:x=>`${x.artist||'未知歌手'} · ${x.playlist||'未分类'}`},
    maps:{list:r=>r.maps.places,fields:[['name','地点名称','text'],['address','地址','text'],['label','标签','text'],['routeFrom','出发地 / 路线','text'],['transport','交通方式','text'],['note','到访备注','textarea']],title:x=>x.name,subtitle:x=>`${x.address||''} · ${x.label||'地点'}`}
  };
  function schemaRow(key,item,index){const schema=schemas[key];return`<button class="v455-real-row" onclick="v455PhoneEditSchema(${A(key)},${A(item.id)})"><span>${E(appName(key).slice(0,1))}</span><div><b>${E(schema.title(item)||'未命名')}</b><small>${E(schema.subtitle(item)||'')}</small></div><i>›</i></button>`}
  function openSchema(key){const schema=schemas[key],list=schema.list(real()),special=key==='notes'?`<div class="v455-note-grid">${list.map(item=>`<button onclick="v455PhoneEditSchema('notes',${A(item.id)})"><small>${item.pinned==='是'?'置顶 · ':''}${E(displayTime(item.updatedAt||item.createdAt))}</small><b>${E(item.title||'便笺')}</b><p>${E(item.body||'')}</p></button>`).join('')}</div>`:key==='market'?`<div class="v452-order-stats"><span><b>${list.filter(x=>x.status==='待付款').length}</b><small>待付款</small></span><span><b>${list.filter(x=>x.status==='运输中').length}</b><small>运输中</small></span><span><b>${list.length}</b><small>全部订单</small></span><span><b>${list.filter(x=>x.status==='退款').length}</b><small>退款</small></span></div><section class="v452-shop-banner"><small>ORDERS</small><b>订单、金额、物流与退款</b></section>${list.length?`<div class="v455-real-list">${list.map((x,i)=>schemaRow(key,x,i)).join('')}</div>`:''}`:`<div class="v455-real-list">${list.map((x,i)=>schemaRow(key,x,i)).join('')}</div>`;shell(appName(key),`${reactionMarkup(key,`${list.length} 项${appName(key)}数据`)}<div class="v455-phone-toolbar"><span>${list.length} 项</span>${allowEdit()?`<button onclick="v455PhoneEditSchema(${A(key)},'')">＋ ${key==='schedule'?'日程':key==='market'?'订单':key==='browser'?'书签':key==='music'?'歌曲':key==='maps'?'地点':'便笺'}</button>`:''}</div>${list.length?special:empty(appName(key).slice(0,1),`还没有${appName(key)}数据`,`使用对应字段创建${appName(key)}内容。`,allowEdit()?`v455PhoneEditSchema(${A(key)},'')`:'','新建')}`)}
  window.v455PhoneEditSchema=function(key,id){if(!requireEdit())return;const schema=schemas[key],list=schema.list(real()),item=list.find(x=>x.id===id)||{};overlay(`${overlayHead(id?'EDIT DATA':'NEW DATA',`${id?'编辑':'新建'}${appName(key)}`)}${schema.fields.map(([field,label,type,options])=>`<label class="v455-sheet-field"><span>${E(label)}</span>${type==='textarea'?`<textarea id="v455Schema_${field}">${E(item[field]||'')}</textarea>`:type==='select'?`<select id="v455Schema_${field}">${options.map(opt=>`<option ${S(item[field])===opt?'selected':''}>${E(opt)}</option>`).join('')}</select>`:`<input id="v455Schema_${field}" type="${type}" value="${AT(item[field]||'')}">`}</label>`).join('')}${actions(`v455PhoneSaveSchema(${A(key)},${A(id)})`,'保存',id?`<button class="danger" onclick="v455PhoneDeleteSchema(${A(key)},${A(id)})">删除</button>`:'')}`)};
  window.v455PhoneSaveSchema=function(key,id){const schema=schemas[key],list=schema.list(real()),values={};for(const [field,,type] of schema.fields){const el=document.getElementById(`v455Schema_${field}`);values[field]=type==='number'?Number(el?.value)||0:S(el?.value).trim()}const required=key==='maps'?values.name:values.title||values.product;if(!required)return toast('请填写主要名称');const item=list.find(x=>x.id===id);if(item)Object.assign(item,values,{updatedAt:NOW()});else list.unshift({id:uid(key),...values,createdAt:NOW(),updatedAt:NOW()});save();closeOverlay();openSchema(key)};
  window.v455PhoneDeleteSchema=function(key,id){if(!confirm(`删除这项${appName(key)}数据并移入回收站？`))return;const list=schemas[key].list(real()),i=list.findIndex(x=>x.id===id);if(i>=0){store().trash.push({id:uid('phone_trash'),kind:'real-app',snapshot:clone(list[i]),original:{app:key,index:i},deletedAt:NOW()});list.splice(i,1)}save();closeOverlay();openSchema(key)};
  function openWeather(){const w=real().weather;shell('天气',`${reactionMarkup('weather',`${w.location||'未设置地点'} ${w.condition||''}`)}<div class="v455-phone-toolbar"><span>场景天气</span>${allowEdit()?'<button onclick="v455PhoneEditWeather()">编辑</button>':''}</div><section class="v452-weather-card"><small>${E(w.location||'当前位置未设置')}</small><strong>${E(w.condition||'—')} ${E(w.temperature||'')}</strong><p>${E(w.feelsLike?`体感 ${w.feelsLike}`:'等待设置场景天气')}</p><div>${array(w.forecast).slice(0,3).map(x=>`<span>${E(x.time||'稍后')}　${E(x.condition||'—')}</span>`).join('')||'<span>现在　—</span><span>稍后　—</span><span>明日　—</span>'}</div></section>`)}
  window.v455PhoneEditWeather=function(){if(!requireEdit())return;const w=real().weather;overlay(`${overlayHead('EDIT WEATHER','编辑场景天气')}<label class="v455-sheet-field"><span>世界内地点</span><input id="v455WeatherLocation" value="${AT(w.location)}"></label><label class="v455-sheet-field"><span>当前天气</span><input id="v455WeatherCondition" value="${AT(w.condition)}"></label><label class="v455-sheet-field"><span>温度</span><input id="v455WeatherTemp" value="${AT(w.temperature)}"></label><label class="v455-sheet-field"><span>体感</span><input id="v455WeatherFeels" value="${AT(w.feelsLike)}"></label><label class="v455-sheet-field"><span>预报（每行：时段｜天气）</span><textarea id="v455WeatherForecast">${E(array(w.forecast).map(x=>`${x.time}｜${x.condition}`).join('\n'))}</textarea></label>${actions('v455PhoneSaveWeather()')}`)};window.v455PhoneSaveWeather=function(){const w=real().weather;w.location=document.getElementById('v455WeatherLocation')?.value.trim();w.condition=document.getElementById('v455WeatherCondition')?.value.trim();w.temperature=document.getElementById('v455WeatherTemp')?.value.trim();w.feelsLike=document.getElementById('v455WeatherFeels')?.value.trim();w.forecast=S(document.getElementById('v455WeatherForecast')?.value).split(/\n+/).filter(Boolean).map(line=>{const [time,condition]=line.split(/[｜|]/);return{time:S(time).trim(),condition:S(condition).trim()}});w.updatedAt=NOW();save();closeOverlay();openWeather()};
  function openSettings(){const s=store();shell('设置',`${reactionMarkup('settings','当前手机设置')}<div class="v455-settings-list">${owner==='user'&&session().mode==='browse'?`<button onclick="v456PhoneOpenCompletion()"><span>✦</span><div><b>智能完善我的手机</b><small>按白名单单选、多选或全部追加</small></div><i>›</i></button>`:''}<button onclick="toast('保持现有手机桌面与壁纸设置')"><span>◐</span><div><b>显示与壁纸</b><small>${E(s.phoneSettings.appearance||'深色')}</small></div><i>›</i></button><button onclick="v455PhoneTrashSettings()"><span>⌁</span><div><b>回收站</b><small>${s.phoneSettings.trashRetention==='permanent'?'永久保留':'保留 30 天'}</small></div><i>›</i></button><button onclick="v455PhoneOpenContacts()"><span>联</span><div><b>联系人白名单</b><small>${s.whitelist.length} 个联系人</small></div><i>›</i></button><button onclick="v455PhoneReset()"><span>重</span><div><b>重置手机</b><small>精确选择范围</small></div><i>›</i></button></div>`)}

  /* Keep mature learning/square components; all old linked panels are removed by core. */
  function openPhoneApp(nextOwner,key){owner=nextOwner;app=key;threadId='';postId='';photoId='';try{v43ActivePhoneOwner=owner}catch{}
    if(key==='messages')return openMessages();if(key==='moments')return openMoments();if(key==='gallery')return openGallery();if(key==='wallet')return openWallet();if(schemas[key])return openSchema(key);if(key==='weather')return openWeather();if(key==='settings')return openSettings();if(['learning','square'].includes(key)&&basePhoneApp){const result=basePhoneApp(owner,key);setTimeout(()=>{document.querySelectorAll('.v452-linked-records').forEach(x=>x.remove());V.cleanObsoletePhoneArtifacts()},80);return result}return shell(appName(key),empty('·','暂无可用页面',`${appName(key)}数据没有被删除。`))}
  window.v43OpenPhoneApp=openPhoneApp;window.openSimPhoneApp=openPhoneApp;try{v43OpenPhoneApp=openPhoneApp;openSimPhoneApp=openPhoneApp}catch{}

  /* Public route aliases */
  Object.assign(window,{v455PhoneOpenThread:openThread,v455PhoneOpenGallery:openGallery,v455PhoneOpenWallet:openWallet,v455PhoneOpenMoments:openMoments});
  save();
})();
